ToDo:

- Merge add-harvest-log-category changes
- Merge add-harvest-lib changes


  - DONE: add harvest.js to lib with:
    - getHarvestUnits(cropName)
    - createHarvestLog(???)
    - getHarvestLog(???)
    - deleteHarvestLog(???)



   - Update docs 
     - example for import at top
       - use: import * as farmosUtil from '@libs/farmosUtil/farmosUtil';
       - update whole example block to put in Vue instance.
     - Check on {Object} [blah=null] for JSDoc
       - Shows up in fetchPlants, possibly others as well.
       - @param {Object|null} [options=null] 
     - Some wonkiness with where some things appear in the docs
       - Seems like extractQuantity should be in utilities
       - Utilities as a module is docked different than others?
   
All work must be in an entrypoint...
  - Make 3 entrypoints?
    - FD2-Tutorial
    - FD2-HandsOn
    - FD2-App

Tutorial
  - Preliminaries
    - update development
    - run `reinstallFD2Module.bash`
    - run `installDB.bash` 

  - Learned about fetch and APIs...
  - Issue of repeated code...
  - FD2 has a library of JS functions for common operations.
    - Consistency
    - Abstraction
    - Maintenance
    - Encapsulation / Information Hiding
    - Testing

- Using the library functions (must happen in an entrypoint)
  - Import it

  - Look at docs...
    - Find function for
      - fetching crops
      - discuss it here in more detail
        - how to call it.
        - what it returns
    - Convert the fetch of crops to a function call
      - Give the code to add and where.

  MOVE TO HANDS ON IF THIS GETS TOO LONG
  - Repeat for:
    - fetching plants
      - issue here with providing parameters...
        - or introduce named parameters?


Class Time
- Review use of libs and docs
- Show lib folder and test files (just to see they exist)
- Show creating Harvest log in the UI
  - Explain References to
    - Quantity
        - measures
    - Plant Asset
  - This is part of why FD2 is needed!!!
    - farmOS is unopinionated and is very good.
    - But not tailored to specific applications.

- Show API result
  - Point out References

Hands-On:

- Have them create a harvest log in UI
  - Specify the plant, quantity, units etc..
- Create a Quantity using library function...
  - Using fixed values to test.
  - See it in the UI after creating via code.

Don't think I need this but its here:


  ```
  const quant = farm.quantity.create({
    type: 'quantity--standard',
    attributes: {
      measure: 'weight',
      value: {
        decimal: 5.5,
      },
      label: 'Harvest',
    },
    relationships: {
      units: {
        type: 'taxonomy_term--unit',
        id: unitMap.get('POUND').id,
      },
    },
  });
  ```

Application:
- Get the harvest units for the selected crop and populate the list.
  - There is a function!!
- Create the quantity by adapting the hands-on to use the data.
- Create the harvest log
  - There is a function!


