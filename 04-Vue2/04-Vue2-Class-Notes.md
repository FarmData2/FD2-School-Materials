
Big picture... where they are where they are going...


- FarmData2 forms will not use the `<form>` element as in the tutorial.
  - But everything else is applicable.
  - We also use `v-on` instead of `@` because it is more readable for newcomers.

- push as an array operation in JS.
  
- use of "" vs '' in HTML (template) vs JS/Vue (script)
  - nesting of "'...'" and '"..."'