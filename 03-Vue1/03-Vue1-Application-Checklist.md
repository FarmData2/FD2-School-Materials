- [x] - The options in the "Crop" select are rendered from an array in Vue `data` using `v-for`.
- [x] - The rows in the "Plants" table are rendered from an array in the Vue `data` using `v-for`.
- [x] - The options in the "Units" select are rendered from an array in the Vue `data` using `v-for`.
- [x] - Work is spread across multiple commits.

Things to look for and comment on in the PR:
- Naming of the `data` properties.
- Use of arrays of objects vs strings for crops or units in `data`.
- `plant.id` is used for `key`
- Array or object structure for plants in `data`.