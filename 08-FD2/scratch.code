<template>
  <div
    id="test-push"
    data-cy="test-push"
  >
    <button v-on:click="createQuantity">Create Quantity</button>
    <br /><br />
    <button v-on:click="createHarvestLog">Create Harvest Log</button>
  </div>
</template>

<script>
import * as farmosUtil from '@libs/farmosUtil/farmosUtil';
export default {
  data() {
    return {};
  },
  methods: {
    async createQuantity() {
      console.log('creating quantity');

      const farm = await farmosUtil.getFarmOSInstance();
      const unitMap = await farmosUtil.getUnitToTermMap();

      const quant = farm.quantity.create({
        type: 'quantity--standard',
        attributes: {
          measure: 'weight',
          value: {
            decimal: 5.5,
          },
          label: 'Harvest',
        },
        relationships: {
          units: {
            type: 'taxonomy_term--unit',
            id: unitMap.get('POUND').id,
          },
        },
      });

      const result = await fetch(

      )

      //const result = await farm.quantity.send(quant);
      console.log(quant);
    },
    async createHarvestLog() {
      console.log('creating harvest log');
    },
  },
};
</script>

<style>
/* import some styling that applies to all FD2 entry points */
@import url('@css/fd2-mobile.css');
</style>
