
- emphasize distinction between crop and plant.

- The added `<div>` around multiple elements to allow them all to be easily hidden with the `v-if`.

- JavaScript comparison operators are similar to Java
  - `===` vs `==` vs `=` 
  - `!=`

- Don't forget `this`.
  - You are going to forget `this`.
  - code in the `<script>` must use `this.` to access data and methods.
  - code in the `<template>` cannot use `this.` to access data and methods.
  

