
## Grading Checklist

- [x] - Plants are fetched using the `getPlantAssets` function.
- [x] - The `unitList` is initialized to an empty array.
- [x] - The "Unit" select is populated using the `getHarvestUnits` function with the current crop.
- [x] - Clicking submit uses `createHarvestLog` to create a harvest log with the data entered into the form.
- [x] - `console.log` statements used for testing have been removed.
- [x] - Work is spread across multiple commits.

## Common Feedback

- The comment should also be submitted as part of the harvest log.  See the documentation for `createHarvestLog` for where the argument for the comment goes.
- This is not a proper call to the `createHarvestLog` function. Double check the documentation for `createHarvestLog` to see which parameters are required and the order in which they need to appear.