# 03 - Vue1 - Class Notes

**DRAFT**

- modify the array using the Vue Devtools
  - remove an item.
  - add an item
    - need to quote the attribute name and values

- split quantity and item in object.
- Put items into a table.

- something that uses `index` with `v-for`
  - number the list with a .?

- put radio button in first column?
  - show them using AI in class?
  - Then they've seen it for the homework. 