
tutorial:
use shopping list app.

cd to 10-FD2-Tutorial-Starter directory
open `index.html`
open `index.e2e.cy.js`
npx cypress open

In class
- Finding assertions and actions
  - references
  - Demo AI

- Testing with components in FarmData2
  - seeing data-cy value in the docs.
  - accessing nested values???
    - .get(the component data-cy).find(the element data-cy).should
  


cy.pause() - good for debugging.

